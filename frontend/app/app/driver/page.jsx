'use client';import{useEffect,useState}from'react';import Link from'next/link';import{apiFetch}from'../../../lib/api';import{EmptyState,ErrorState,LoadingState,PageHeader,StatusBadge}from'../../../components/ui/LoadlyxUI';export default function DriverHome(){const[data,setData]=useState(null);const[error,setError]=useState('');useEffect(()=>{apiFetch('/loadlyx-connect/workspace').then(setData).catch(e=>setError(e.message))},[]);const current=data?.loads?.find(l=>l.assignment&&l.assignment.status!=='COMPLETE');return <main className="container grid lx-driver-home" style={{gap:24}}><PageHeader eyebrow="Driver Home" title="One safe next action at a time." description="Current work, milestones, documents, earnings, fuel, and supportâ€”optimized for quick mobile use."/>{error?<ErrorState message={error}/>:!data?<LoadingState label="Loading driver workspace"/>:current?<><section className="lx-driver-current"><div><StatusBadge tone="success">{current.assignment.status.replaceAll('_',' ')}</StatusBadge><h2>{current.title}</h2><p>{current.originCity} â†’ {current.destinationCity}</p></div><Link className="btn lx-btn-lg" href="/app/connect#loads">Open next action</Link></section><section className="lx-driver-actions">{[['Navigation','Open active route','/app/connect#tracking'],['Documents','BOL, POD and photos','/app/connect#documents'],['Messages','Load communication','/app/connect#messages'],['Earnings','Settlements and payouts','/app/marketplace-transactions'],['Fuel','Route fuel estimate','/app/services#fuel'],['Support','Get operational help','/resources']].map(([title,body,href])=><Link href={href} key={title}><strong>{title}</strong><span>{body}</span></Link>)}</section><p className="lx-inline-notice">Push-notification hooks are prepared through the background worker. Delivery remains configuration-required until a push provider is connected.</p></>:<EmptyState title="No active assigned load" description="Your next accepted assignment will appear here." actionHref="/loadboard" actionLabel="View load board"/>}</main>}

